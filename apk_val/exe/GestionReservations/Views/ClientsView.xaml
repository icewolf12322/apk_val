<UserControl x:Class="GestionReservations.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:GestionReservations.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:ClientsViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Carnet d'Adresses Clients" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,16"/>

        <!-- Barre de recherche simple et efficace -->
        <TextBox Grid.Row="1"
                 materialDesign:HintAssist.Hint="Rechercher un client (nom, email, téléphone...)"
                 materialDesign:TextFieldAssist.HasClearButton="True"
                 materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"
                 materialDesign:TextFieldAssist.TextFieldCornerRadius="8"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,24">
        </TextBox>

        <!-- Grille des clients, avec édition en ligne -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding ClientsView}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom Complet" Binding="{Binding NomComplet, UpdateSourceTrigger=PropertyChanged}" Width="2*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                <DataGridTextColumn Header="Téléphone" Binding="{Binding Telephone, UpdateSourceTrigger=PropertyChanged}" Width="1*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Width="2*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" 
                Style="{StaticResource MaterialDesignRaisedButton}"
                Command="{Binding SaveChangesCommand}"
                HorizontalAlignment="Right"
                Margin="0,24,0,0"
                Width="250"
                Height="48"
                Content="Enregistrer les modifications"
                ToolTip="Sauvegarde les changements effectués dans la base de données."/>
    </Grid>
</UserControl>