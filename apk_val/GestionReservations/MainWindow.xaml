<Window x:Class="GestionReservations.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewModels="clr-namespace:GestionReservations.ViewModels"
        xmlns:views="clr-namespace:GestionReservations.Views"
        mc:Ignorable="d"
        Title="Tableau de Bord d'Administration Hôtelière" Height="768" Width="1024"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel, IsDesignTimeCreatable=False}">
    <Window.Resources>
        <!-- DataTemplates pour lier les ViewModels à leurs Vues respectives -->
        <DataTemplate DataType="{x:Type viewModels:ReservationsViewModel}">
            <views:ReservationsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:ClientsViewModel}">
            <TextBlock Text="Module Clients (à implémenter)" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:TranslationsViewModel}">
            <views:TranslationsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:SettingsViewModel}">
            <TextBlock Text="Module Paramètres (à implémenter)" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Panneau de navigation principal, simple et visuel pour le groom -->
        <ListBox x:Name="NavigationMenu" Grid.Column="0"
                 Style="{StaticResource MaterialDesignNavigationRailListBox}"
                 SelectedIndex="{Binding SelectedIndex}">
            <!-- La sélection d'un item changera la vue affichée dans le ContentControl -->
            <ListBoxItem Content="Réservations" ToolTip="Gestion des réservations entrantes">
                <ListBoxItem.Tag>
                    <materialDesign:PackIcon Kind="CalendarClock" Width="24" Height="24" />
                </ListBoxItem.Tag>
            </ListBoxItem>
            <ListBoxItem Content="Clients" ToolTip="Carnet d'adresses des clients">
                <ListBoxItem.Tag>
                    <materialDesign:PackIcon Kind="AccountGroup" Width="24" Height="24" />
                </ListBoxItem.Tag>
            </ListBoxItem>
            <ListBoxItem Content="Traductions" ToolTip="Éditer les textes de l'application mobile (common.json)">
                <ListBoxItem.Tag>
                    <materialDesign:PackIcon Kind="Translate" Width="24" Height="24" />
                </ListBoxItem.Tag>
            </ListBoxItem>
            <ListBoxItem Content="Paramètres" ToolTip="Configuration de l'application">
                <ListBoxItem.Tag>
                    <materialDesign:PackIcon Kind="Cog" Width="24" Height="24" />
                </ListBoxItem.Tag>
            </ListBoxItem>
        </ListBox>

        <!-- Zone principale où les vues (UserControls) seront injectées -->
        <ContentControl Grid.Column="1" Margin="24" Content="{Binding CurrentViewModel}" />

        <!-- Barre de statut avec message simple et code couleur -->
        <StatusBar Grid.Column="0" Grid.ColumnSpan="2" VerticalAlignment="Bottom">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CheckCircle" Foreground="Green" Margin="0,0,8,0" />
                    <TextBlock Text="Prêt" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>
