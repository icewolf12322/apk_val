<UserControl x:Class="GestionReservations.Views.TranslationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:GestionReservations.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:TranslationsViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Éditeur de textes pour l'application mobile" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,16"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Entries}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Clé de traduction" Binding="{Binding Key}" IsReadOnly="True" Width="250" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                <DataGridTextColumn Header="Texte affiché dans l'application" Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" 
                Style="{StaticResource MaterialDesignRaisedButton}"
                Command="{Binding SaveAndUploadCommand}"
                HorizontalAlignment="Right"
                Margin="0,24,0,0"
                Width="350"
                Height="48"
                Content="Enregistrer et Mettre à jour le mobile"
                ToolTip="Sauvegarde les modifications et envoie le fichier pour l'application mobile."/>
    </Grid>
</UserControl>