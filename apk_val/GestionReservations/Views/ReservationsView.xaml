<UserControl x:Class="GestionReservations.Views.ReservationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:GestionReservations.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:ReservationsViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="700" d:DesignWidth="900"
             Background="Transparent">

    <UserControl.Resources>
        <!-- Style pour les boutons d'action, conformes à l'UX "Groom-Friendly" -->
        <Style x:Key="ActionButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- DataTemplate pour une vignette de réservation -->
        <DataTemplate x:Key="ReservationCardTemplate" DataType="{x:Type viewModels:ReservationCardViewModel}">
            <materialDesign:Card Width="340" Margin="12" Padding="16" UniformCornerRadius="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Entête : Nom du client -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="Account" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="{DynamicResource PrimaryHueMidBrush}" Width="28" Height="28"/>
                        <TextBlock Text="{Binding Reservation.ClientNomComplet}" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Separator Grid.Row="1" Margin="0,0,0,12"/>

                    <!-- Détails de la réservation -->
                    <StackPanel Grid.Row="2" Margin="4,0,0,20" TextElement.FontSize="14">
                        <StackPanel Orientation="Horizontal" Margin="0,6">
                            <materialDesign:PackIcon Kind="CalendarRange" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource MaterialDesignBodyLight}" />
                            <TextBlock>
                                <Run Text="Du" FontWeight="SemiBold"/>
                                <Run Text="{Binding Reservation.DateArrivee, StringFormat='dd MMMM'}"/>
                                <Run Text="au"/>
                                <Run Text="{Binding Reservation.DateDepart, StringFormat='dd MMMM'}"/>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,6">
                            <materialDesign:PackIcon Kind="Bed" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource MaterialDesignBodyLight}" />
                            <TextBlock Text="{Binding Reservation.TypeChambre}" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,6">
                            <materialDesign:PackIcon Kind="AccountMultiple" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource MaterialDesignBodyLight}" />
                            <TextBlock>
                                <Run Text="{Binding Reservation.NombrePersonnes}"/>
                                <Run Text="personne(s)"/>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>

                    <!-- Boutons d'action -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" >
                        <Button Style="{StaticResource ActionButton}" Background="#4CAF50" ToolTip="Valider la réservation" Command="{Binding ValiderCommand}">
                            <materialDesign:PackIcon Kind="CheckBold" Width="24" Height="24"/>
                        </Button>
                        <Button Style="{StaticResource ActionButton}" Background="#FF9800" Margin="8,0" ToolTip="Signaler un problème / Demander une correction" Command="{Binding SignalerProblemeCommand}">
                            <materialDesign:PackIcon Kind="Alert" Width="24" Height="24"/>
                        </Button>
                        <Button Style="{StaticResource ActionButton}" Background="#F44336" ToolTip="Refuser la réservation (complet, etc.)" Command="{Binding RefuserCommand}">
                            <materialDesign:PackIcon Kind="CloseThick" Width="24" Height="24"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Onglets de filtre par type de chambre -->
        <TabControl Grid.Row="0" Margin="8,8,8,0"
                    ItemsSource="{Binding Filters}"
                    SelectedItem="{Binding SelectedFilter}"
                    Style="{StaticResource MaterialDesignNavigatilRailTabControl}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="14" FontWeight="SemiBold" Padding="12,8"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>

        <!-- Conteneur pour les vignettes de réservation -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding ReservationsView}" ItemTemplate="{StaticResource ReservationCardTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>